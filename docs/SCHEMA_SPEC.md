# SchemaSync Metadata Specification

## Objects
- `TableMetadata`: Encapsulates schema and table identification, along with column and index lists.
- `ColumnMetadata`: Represents column definitions including JDBC types, nullability, precision, scale, and primary key participation.
- `IndexMetadata`: Tracks unique and non-unique composite table indices.
